version: '3'
services:
  gitolite:
    build:
      context: .
      dockerfile: ./docker/Dockerfile-gitolite
      args:
        - ADMIN_PUB_KEY
    ports:
      - "62666:22"
    restart: always
    volumes:
      - gitdata:/home/git
volumes:
  gitdata:
